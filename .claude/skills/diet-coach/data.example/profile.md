# Profile — personal health context (TEMPLATE)

> Copy this to `data/profile.md` and fill it in with your own data. `profile.md`
> is **gitignored** — it is never committed. The skill and the code only
> reference it; they contain no concrete values.

- Sex: `<male/female>`. `<height>` cm, `<age>` years, starting `<kg>` kg, goal `<kg>` kg.
- **Calorie band `<min>`–`<max>` kcal/day. NEVER persistently below this** (not
  starvation). Set the same band in `data/config.json` too.
- **Sensitivity / medical constraint**, if any (e.g. gallbladder issues →
  moderate, day-distributed fat; food intolerance; allergy). Leave empty if none.
- High protein at every main meal; increase fiber gradually.
- Other constant context: type of work (sedentary?), exercise habit, BMR estimate.
- This is a supplement, not medical advice.
